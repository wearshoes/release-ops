# Generic Signing Command

Use only when a supported stack exposes a signable artifact but no specialized signing extension applies. The command is stored as `executable + args`, runs with `shell:false`, and receives only declared credential roles.
