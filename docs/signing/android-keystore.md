# Android Keystore Signing

Select one or more Android build units and map the `keystore`, `store-password`, `key-alias`, and `key-password` roles to Actions Secret names. Credential values never enter configuration.
